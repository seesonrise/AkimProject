1. Create .venv (python -m venv ./.venv)
2. Start venv (./.venv/Scripts/Activate.ps1 to PowerShell or activate.bat to cmd)
3. Install packages (pip install -r requirements.txt)
4. And run site (python run.py)
